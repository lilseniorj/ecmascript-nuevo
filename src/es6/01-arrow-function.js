function square(num) { // funcion
  return num * num;
}

const square = (num) => { // funcion de forma arrow function
  return num * num;
}

const square = num => num * num; // funcion con el return implicito